#include "human.h"

int Human::move(_hex& board)
{
    int temp=0;
    static bool first = true;
    if (first) {

        first = false;
    }
    else;


    board.set_node(temp, color);
    return temp;
}
